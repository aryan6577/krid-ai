# Points the chat assistant at the sportsync-backend server.
# Defaults to http://localhost:5001 if not set — only needed if you run the
# backend on a different host/port.
VITE_API_URL=http://localhost:5001
